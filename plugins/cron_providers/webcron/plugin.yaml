name: webcron
description: >-
  Webcron — NAS-mediated managed cron provider for scale-to-zero hosted agents.
  Delegates the "wake me at time T" trigger to Hackit infrastructure so an idle
  gateway can scale to zero and still fire cron jobs. The agent computes each
  job's next-fire time and asks NAS to arm a one-shot; NAS calls the agent back
  at fire time over an authenticated webhook. Inert unless cron.provider=webcron.
version: 1.0.0
author: Hackit
